---
"@aliou/pi-processes": minor
---

Rename tool and commands from `processes` to `process`. Add /process:stream, /process:logs, /process:kill, /process:clear commands with autocomplete. Add settings support via /process:settings. Auto-stream logs widget on process start.
